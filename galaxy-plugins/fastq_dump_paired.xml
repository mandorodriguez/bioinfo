<tool id="fastq_dump_paired" name="FASTQ Dump paired downloader" version="0.1">
    <description>Downloads a paired read by it's accession number</description>
    <requirements>
       <!-- Should just use the fastq-dump that's installed in the path -->
       <requirement type="binary">fastq-dump</requirement>
    </requirements>
    <command>
## The command is a Cheetah template which allows some Python based syntax.
## Lines starting hash hash are comments. Galaxy will turn newlines into spaces
## ! We only have a simple single command here.
fastq-dump --log-level fatal --split-3 --accession ${accession_number}
    </command>

    <inputs>
        <param name="accession_number" size="10" type="text" value="chr7" label="Chromosome"/>
    </inputs>
    <outputs>
       <data format="fastq" from_work_dir="${accession_number}._1.fastq" name="outfile_1" />
       <data format="fastq" from_work_dir="${accession_number}._2.fastq" name="outfile_2" />
    </outputs>
    
    <tests>
    <!-- Empty -->
    </tests>

    <help>
**What it does**

Performs a fastq-dump with the split-3 option.

::

  fastq-dump --log-level fatal --split-3 --accession ${accession_number}
    </help>
</tool>